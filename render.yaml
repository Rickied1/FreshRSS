services:
  - type: web
    name: freshrss
    env: docker
    repo: https://github.com/Rickied1/FreshRSS
    branch: edge
    dockerfilePath: docker/Dockerfile
    buildCommand: docker build -t freshrss -f docker/Dockerfile docker
